/// <reference types="astro/client" />

/**
 * True when the private Founders Grotesk submodule is checked out at
 * src/assets/fonts/. Inlined at build time by Vite (see astro.config.mjs), so
 * public clones without access to the font build without it.
 */
declare const __HAS_FOUNDERS_GROTESK__: boolean;
