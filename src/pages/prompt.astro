---
import BaseLayout from "../layouts/BaseLayout.astro";
import Nav from "../components/Nav.astro";
import { getEditableTemplate } from "../lib/chatPrompt";

const current = await getEditableTemplate();
---

<BaseLayout title="Éclaire - Édition du prompt">
  <Nav />
  <main class="prompt-editor">
    <header>
      <h1>Prompt système du chat</h1>
      <p>
        Ce texte est envoyé comme instructions au modèle à chaque question.
        Les modifications s'appliquent immédiatement (sans redéploiement).
        Le placeholder <code>[Insère ici le contenu de l'annuaire en JSON]</code>
        est obligatoire : il est remplacé par l'annuaire des organisations.
      </p>
      <p class="meta">
        Source actuelle :
        <strong
          >{
            current.source === "db"
              ? `modifié (${current.updatedAt ? new Date(current.updatedAt).toLocaleString("fr-CH") : ""})`
              : "défaut (fichier versionné)"
          }</strong
        >
      </p>
    </header>

    <textarea id="prompt-content" spellcheck="false">{current.content}</textarea>

    <div class="actions">
      <button id="save-btn" type="button">Enregistrer</button>
      <button id="reset-btn" type="button" class="secondary">
        Rétablir le défaut
      </button>
      <span id="status" role="status"></span>
    </div>
  </main>
</BaseLayout>

<script>
  const textarea = document.getElementById(
    "prompt-content",
  ) as HTMLTextAreaElement;
  const saveBtn = document.getElementById("save-btn") as HTMLButtonElement;
  const resetBtn = document.getElementById("reset-btn") as HTMLButtonElement;
  const status = document.getElementById("status") as HTMLSpanElement;

  async function submit(payload: object) {
    saveBtn.disabled = true;
    resetBtn.disabled = true;
    status.textContent = "Enregistrement…";
    status.className = "";
    try {
      const res = await fetch("/api/prompt", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify(payload),
      });
      const data = await res.json();
      if (!res.ok) throw new Error(data.error ?? `Erreur ${res.status}`);
      status.textContent = "Enregistré ✓ — actif immédiatement";
      status.className = "ok";
      if ("reset" in payload) {
        const fresh = await fetch("/api/prompt").then((r) => r.json());
        textarea.value = fresh.content;
      }
    } catch (err) {
      status.textContent =
        err instanceof Error ? err.message : "Échec de la sauvegarde";
      status.className = "error";
    } finally {
      saveBtn.disabled = false;
      resetBtn.disabled = false;
    }
  }

  saveBtn.addEventListener("click", () =>
    submit({ content: textarea.value }),
  );
  resetBtn.addEventListener("click", () => {
    if (confirm("Rétablir le prompt par défaut (fichier versionné) ?")) {
      submit({ reset: true });
    }
  });
</script>

<style>
  .prompt-editor {
    max-width: 60rem;
    margin: 0 auto;
    padding: 1.5rem 1rem 3rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  header p {
    color: rgb(var(--color-2, 80, 80, 80));
    line-height: 1.5;
  }
  header code {
    background: rgba(0, 0, 0, 0.06);
    padding: 0.1rem 0.3rem;
    border-radius: 4px;
  }
  .meta {
    font-size: 0.9rem;
  }
  #prompt-content {
    width: 100%;
    min-height: 65vh;
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: 0.85rem;
    line-height: 1.45;
    padding: 0.75rem;
    border: 1px solid rgba(0, 0, 0, 0.2);
    border-radius: 8px;
    box-sizing: border-box;
    resize: vertical;
  }
  .actions {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    flex-wrap: wrap;
  }
  .actions button {
    padding: 0.55rem 1.2rem;
    border-radius: 8px;
    border: none;
    background: rgb(var(--color-1, 0, 0, 0));
    color: rgb(var(--bg-color-1, 255, 255, 255));
    font-weight: 600;
    cursor: pointer;
  }
  .actions button.secondary {
    background: transparent;
    color: rgb(var(--color-1, 0, 0, 0));
    border: 1px solid rgba(0, 0, 0, 0.3);
  }
  .actions button:disabled {
    opacity: 0.5;
    cursor: wait;
  }
  #status.ok {
    color: #1a7f37;
  }
  #status.error {
    color: #c62828;
  }
</style>
